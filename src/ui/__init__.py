"""Dialog UI and head-top HUD: serialized-field unpacking for MonoBehaviours
that carry no typetree.

- ``talk``: the field-by-field byte reader, with "zero bytes left over" as its
  self-evident criterion.
"""
