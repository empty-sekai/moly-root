"""现象资产包：游戏把天气叫「现象」——时段亮度、环境配置与其视听表现的提取。

- `config`：环境配置资产与后处理档案的摊平。
- `effects`：粒子效果 prefab，走与其它特效包同一套发射器编码。
- `texarray`：贴图数组的逐层导出与采层参数。
- `meshes`：模型资产与网格发射器的几何，导出为 glTF 二进制。
- `timeline`：驱动某个现象的时间轴资产（轨道、剪辑、驱动目标）。
- `audio`：音乐与环境音，经外部解码器解出波形与循环点。
- `environments`：一个现象跨多个包（全局、共享、每站点一个）的成包提取。
"""
