"""站点资产包：九个站点的场景、室内套件、场地物件与它们的落位契约。

- `placement`：九行站点表、世界坐标与格点常量（几何是站点局部的，偏移只在表里）。
- `census`：109 个站点包的分类与清点。
- `geometry`：prefab 节点树、网格、材质与碰撞面的 glTF 导出。
- `navmesh`：出货烘焙的导航网格（瓦片原样搬运，高度网格导成几何）。
- `clips`：动画剪辑（编译曲线解码，绑定路径按 CRC-32 还原节点名）。
- `scenes`：单个包的提取，逐对象记账。
- `sites`：成包提取与索引。
"""
